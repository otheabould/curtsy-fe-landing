import React from 'react';

import './header.scss';
const Header = () => {
    return (
        <header>
            <p className="logo">Curtsy.</p>
        </header>
    );
};

export default Header;
